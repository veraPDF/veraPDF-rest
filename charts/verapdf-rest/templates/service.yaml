apiVersion: v1
kind: Service
metadata:
  name: {{ include "verapdf-rest.fullname" . }}
  labels:
    {{- include "verapdf-rest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "verapdf-rest.selectorLabels" . | nindent 4 }}
